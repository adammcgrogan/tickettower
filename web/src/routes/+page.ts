// The landing page is prerendered, so search engines and link previews get
// real HTML. Anything that reads the URL or the session happens in onMount.
export const prerender = true;
export const ssr = true;
